// laba 2.cpp : Этот файл содержит функцию "main". Здесь начинается и заканчивается выполнение программы.
//

#include <iostream>
#include <string>
#include <iomanip>
#include <typeinfo>
#include <windows.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "RU");
    //zadanie 1 +
    
 /*   cout << "Задание 1\n";
    int x, y, z;
    double rez;

    cout << "Запишите через пробел 3 числа для нахождение их суммы\n";
    cin >> x >> y >> z;
    rez = (double)x + (double)y + (double)z;

    
    cout << typeid(rez).name() << " " << rez << endl;
    system("pause");
    system("cls");*/

    //zadanie 2 +

    //cout << "Задание 2\n";
    //int x, y, z;
    //string st1, st2, st3;

    //cout << "Введите значения для x, y, z через пробел\n";
    //cin >> x >> y >> z;
    //cout << "Введите значения для st1, st2, st3 через пробел\n";
    //cin >> st1 >> st2 >> st3;

    //cout << st1 << setfill('.') << setw(60) << x << "\n";
    //cout << st2 << setfill('.') << setw(60) << y << "\n";
    //cout << st3 << setfill('.') << setw(60) << z << "\n";
    //system("pause");
    //system("cls");


    //zadanie 3 +

    //cout << "Задание 3\n";
    //int x;
    //double y;

    //cout << "Введите целое число и с плавающей точкой \n";
    //cin >> x >> y;
    //cout << "число в 16-ричной системе исчисления числа " + to_string(x) + ", это число: " << hex << x << endl;
    //cout << "число в 8-ричной системе исчисления числа " + to_string(x) + ", это число: " << oct << x << endl;
    //cout << "Число в экспоненциальной форме " + to_string(y) + ", это число: " << scientific << y << endl;
    //system("pause");
    //system("cls");

    //zadanie 4+

    //cout << "Задание 4\n";
    //double x = 1.3456;
    //for (int i = 1; i <= 4; i++) {
    //    cout << setw(10) << right << fixed << setprecision(i) << x << endl;
    //}

    //zadanie 5+

    //cout << "Задание 5\n";
    //setlocale(LC_ALL, "Russian");
    //char str1[5];
    //char str2[3];
    //cin.getline(str1, 5);
    //cin.clear();
    //cin.getline(str2, 3);

    //cout << endl << "str1 = " << str1;
    //cout << endl << "str2 = " << str2 << endl;;
    //system("pause");
    //system("cls");

    ////zadanie 6 +



    //cout << "Задание 6\n";
    //int x1, x2, x3, x4;
    //char y1, y2, y3;
    //cin >> y1 >> x1 >> y2 >> x2 >> y3;
    //cout << x1 << ' ' << x2 << endl;
    //cin >> y1 >> x3 >> y2 >> x4 >> y3;
    //cout << x3 << ' ' << x4 << endl;
    //system("pause");
    //system("cls");

    //cin.setf(ios::);

    //cout << y << endl;
    //char a[] = {'(', ')', ','};

    //string a[] = { "(", ")", "," };
    //for (int i = 0; i < sizeof(y); i++) {
    //    cin >> y[i];
    //    cout << y[i] << endl;
    //}
    //for (int i = 0; i < sizeof(y); i++) {
    //        if (typeid(y[i]).name() == typeid(2).name()) {
    //            cout << typeid(y[i]).name() << " " << y[i] << endl;
    //        
    //    }
    //}
    //cin.getline(y);
    

    ////zadanie 7 +
    
    //cout << "Задание 7\n";
    //char s[50];
    //cin.getline(s, 7, 'g');
    //s[7];
    //cout << s << 'g' << '\n';


    ////zadanie 8 +

/*    cout << "Задание 8\n";
    string hell;
    cin >> hell;
    char s1 = hell[0];
    char s2 = hell[1];
    hell.erase(0, 2);
    cout << s1 << ' ' << typeid(s1).name() << "\n" << s2 << ' ' << typeid(s2).name() << "\n" << hell << ' ' << typeid(hell).name() << "\n";
  */  //system("pause");
    //system("cls");

    
    ////zadanie 9 +

    //cout << "Задание 9\n";
    //string a[5] = { "кто вы?", "Какое ваше имя?", "А фамилия?", "А отчество?", "А пол????" };
    //string b[5];
    ////string b[5] = {"human", "Arman", "Kizogyan", "Argamovich", "man"};
    //cout << "пишите ответы на вопросы на английском языке";
    //for (int i = 0; i <= 4; i++) {
    //    cout << '\n' << a[i] << '\n';
    //    cin >> b[i];
    //    cin.clear();
    //}
    //system("cls");

    //for (int i = 0; i <= 4; i++) {
    //    cout << setw(25) << left << a[i] << endl;
    //    cout << setw(25) << right << b[i] << endl;
    //}
    //system("pause");
    //system("cls");

    ////zadanie 10 +

    //cout << "Задание 10\n";
    //char s;
    //wchar_t s1 = L'q';
    //s = ';';
    //cout << sizeof(s) << ' ' << s << endl;
    //wcout << sizeof(s1) << ' ' << s1 << endl;
    //system("pause");
    //system("cls");
}